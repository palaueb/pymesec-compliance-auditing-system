# Packages

Reusable packages that support the modular monolith belong here.

Examples:

- plugin SDK support
- test harnesses
- shared development tooling

Business domain implementations do not belong here unless they are intentionally reusable packages.

