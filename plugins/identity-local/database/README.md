# Plugin Database

Plugin-owned persistence artifacts for `identity-local` belong here.

The current repository still applies shared migrations from the core app, so
the `identity_local_users` table is introduced through the core migration set
until plugin-owned migration loading is implemented.
