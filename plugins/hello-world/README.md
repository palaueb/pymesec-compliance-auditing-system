# Hello World Plugin

Minimal example plugin used to validate the core plugin skeleton.

It demonstrates:

- local discovery through `plugins/`
- manifest-based runtime declaration
- isolated plugin source under `src/`
- plugin-owned routes under `routes/`

It intentionally does not implement real business logic.
