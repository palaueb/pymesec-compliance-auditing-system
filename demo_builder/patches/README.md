# Generated Patches

This directory is generated by `demo_builder/demo-builder.sh export`.

Each changed source file in the `demo` branch produces one `.patch` file with the same relative path plus the `.patch` suffix.

Do not hand-edit generated patch files unless you are intentionally correcting the patch export.
