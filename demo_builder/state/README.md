# Builder State

This directory stores local workflow metadata generated by `demo_builder/demo-builder.sh`.

The contents are intentionally ignored because they describe the local patch-export state for one checkout, not source code.
