# Core Tests

Placeholder location for core unit, integration, and contract tests.

Current test bootstrap characteristics:

- feature tests use `RefreshDatabase`
- base test setup seeds `Database\\Seeders\\TestDatabaseSeeder`
- test runtime is isolated from the default install profile used in local environments
