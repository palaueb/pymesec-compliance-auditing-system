# Principals

Core abstractions for principals, memberships, and identity-provider interoperability belong here.
Core access-identity references defined by ADR-003.

These classes are core abstractions, not concrete user models.
